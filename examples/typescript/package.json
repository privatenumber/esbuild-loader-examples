{
	"name": "typescript",
	"description": "TypeScript setup using esbuild-loader as an alternative to ts-loader and using tsc for type-checking",
	"main": "dist/main.js",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"build": "npm run typecheck && webpack"
	},
	"devDependencies": {
		"esbuild-loader": "github:privatenumber/esbuild-loader#npm/next",
		"typescript": "^4.9.4",
		"webpack": "^5.75.0",
		"webpack-cli": "^5.0.1"
	}
}
